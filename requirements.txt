# NDA Python Plugin Dependencies
# Install with: pip install -r requirements.txt

# Audio I/O (cross-platform)
sounddevice>=0.4.6
soundcard>=0.4.2
numpy>=1.20.0

# Legacy PyAudio support (optional, sounddevice is preferred)
# PyAudio>=0.2.11

# For WAV file operations
# scipy>=1.7.0  # Optional, for advanced WAV handling

# Cython auto-compilation (optional - falls back to pure Python if not installed)
# Provides 10-50x speedup for DSP-heavy plugins
Cython>=3.0.0
